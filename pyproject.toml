[project]
name = "omni-deck"
version = "2.0.0"
description = "Omni Deck — 专为 Steam Deck (SteamOS/Linux) 打造的五大引擎多世代全能游戏控制中心"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyqt5>=5.15.10",
    "pyqtwebengine>=5.15.6",
]

[tool.uv]
package = false
